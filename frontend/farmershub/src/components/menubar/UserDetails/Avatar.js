import * as React from "react";
import { Avatar } from "@fluentui/react-components";

const ColorBrand = ({init}) => (
  <Avatar color="brand" initials= {init} size={36} name="brand color avatar" />
);

export default ColorBrand
